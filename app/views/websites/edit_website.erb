<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h3> Edit Website </h3>
    <div class="topnav">
      <a class="active" href="/websites">Home</a>
      <form action="/tags" method="POST" autocomplete="off">
        <select id="tags" name="dropdown">
          <% @tag_instances.each do |tag| %>
          <option  value="<%= tag.id %>"><%= tag.content %></option>
          <% end %>
        </select>
        <input id="tags" type="submit" value="submit" class="submitButton">
      </form>
    </div>
    
    <div>
      <form method="post" action="/websites/<%=@website.id%>">
        <input id="hidden" type="hidden" name="_method" value="PATCH">
        <textarea method="PATCH" id="content" name="content" cols="40" rows="5"><%= @website.content %></textarea>
        <br>
        <label>Add any relevant tags</label>
        <input list="tags" name="dropdown">
        <datalist id="tags-add">
          <% @uniq_tags.each do |tag| %>
          <option value="<%= tag %>">
          <% end %>
        </datalist>
        <input type="submit" value="submit" class="submitButton">
      </form>
      <br><br>
      <form action="/websites/<%= @website.id %>/delete" method="post">
        <input type="hidden" name="_method" value="DELETE">
        <input type="submit" value="Delete Website">
      </form>
      <br>
      <a href="/websites">Cancel</a>
    </div>
  </body>
</html>
